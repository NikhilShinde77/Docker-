services:
  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    ports:
      - "8001:9000"
  dev:
    build: ./dev
    ports:
      - "5174:5174"

  redis:
    image: redis
    ports:
      - "6379:6379"
